# Build output -- the exe is regenerated by build.bat
build/

# Self-test report, written on every `build.bat test` run
selftest.txt

# Stray artefacts from experiments
*.obj
*.exe
*.res

# Music dropped in here for testing the player.  Never commit these: they are
# somebody else's recordings, and the game needs no audio files to run.
*.wav
*.mp3
*.mp4
*.m4a
*.flac
*.wma
*.aac
